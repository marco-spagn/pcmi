# DEPRECATED — moved to deploy/helm/pcmi (Helm chart) or deploy/k8s/ (static
# manifests). PR #4 (feat/helm-and-k8s-cleanup) consolidates the duplicate
# k8s/ tree. Please delete this file: `git rm k8s/api-deployment.yaml`.
#
# Migration:
#   - Helm:  helm install pcmi ./deploy/helm/pcmi --namespace pcmi --create-namespace
#   - Plain: kubectl apply -f deploy/k8s/
#
# This empty document is still valid YAML so `kubectl apply -f k8s/` no-ops
# cleanly during the deprecation window.
---
