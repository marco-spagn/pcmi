# DEPRECATED — see k8s/README.md.
# Ingress is intentionally not shipped by the chart: ingress controllers
# (nginx, traefik, cilium, gateway-api) vary too much between clusters to
# template safely. Add your own Ingress / HTTPRoute pointing at the
# {{ release-name }}-pcmi-api Service on port 80 (HTTP) and 50051 (gRPC).
---
