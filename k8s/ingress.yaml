apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pcmi-ingress
spec:
  rules:
  - host: pcmi.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pcmi-api
            port:
              number: 8000