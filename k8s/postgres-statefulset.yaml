# DEPRECATED — see k8s/README.md.
#
# PCMI no longer ships an in-cluster Postgres manifest. Production deployments
# should use a managed Postgres (RDS, Cloud SQL, AlloyDB, Crunchy, etc.) and
# point DATABASE_URL at it via the Helm chart's secrets.DATABASE_URL value.
#
# For local dev, use docker-compose.yml.
---
